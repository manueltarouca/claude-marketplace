{
  "name": "wasteland-kings",
  "version": "0.4.2",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "dev": "vite --port 5173",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run --coverage",
    "lint": "eslint src",
    "postinstall": "node scripts/fetch-assets.mjs"
  },
  "dependencies": { "phaser": "^4.0.0" },
  "devDependencies": {
    "vite": "^6.0.0", "vitest": "^2.1.0", "typescript": "^5.6.0", "eslint": "^9.0.0"
  }
}
